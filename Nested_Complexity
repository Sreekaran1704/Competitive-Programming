Find the time complexity of the following code
int count = 0;
        for (int i = N; i > 0; i /= 2) {
            for (int j = 0; j < i; j++) {
                count += 1;
            }
        }

OPTION A: O(N*N)
OPTION B: O(N*log(N))
OPTION C: O(N * log(log(N)))
OPTION D: O(N)
